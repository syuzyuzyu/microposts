  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3>Followers Users</h3>
        <ol class="microposts">
          <!-- TODO 短く書けない こんな感じ and want to join the view at followers and followings
         render partial: 'microposts/micropost', collection: @microposts
         -->
         <% @users.each do |user| %>
          <%= render partial: 'relationship_user', locals: { user: user } %>
        <% end %>
        </ol>
    </div>
   </div>